import { enUS, de, es, fr, it, nl, pt, ru } from "date-fns/locale";

export const locales = ["en", "pt", "es", "it", "fr", "nl", "de", "ru"];
export const defaultLocale = "en";
export const dateLocales: any = { de, en: enUS, es, fr, it, nl, pt, ru };
